import 'package:flutter/material.dart';

class KalauLoginScreen extends StatelessWidget {
  const KalauLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        height: double.infinity,
        width: double.infinity,
        color: Colors.green,
      ),
    );
  }
}
